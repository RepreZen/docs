#!/bin/bash -e

GEMCMD=gem
if [[ $(uname -s) == Linux ]] ; then
  GEMCMD="sudo gem"
fi

if ! hash bundle 2>/dev/null; then
   if ! hash gem 2>/dev/null; then
      $(dirname $0)/install-ruby
   fi
   $GEMCMD install bundler
fi
